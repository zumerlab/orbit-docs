---
title: "Orbit.resize()"
# banner: 
#  content: |
#    Orbit.resize() has been improved in latest release!
---

import ZumerPlay from '/src/components/ZumerCode.astro';


`Orbit.resize(selectorOrElement)` adapts `.gravity-spot` layouts to a container's width using `ResizeObserver`. It accepts a CSS selector or an element and returns a cleanup function. Call it after the container mounts, and clean up when the container unmounts.

```js
const stopResizing = Orbit.resize(document.querySelector('.wrapper'));
// When removing the container or disposing a component:
stopResizing();
```

Calling `resize()` again for the same container replaces its previous observer. The current site's runtime also updates SVG geometry after DOM, ancestor style and size changes. For CSSOM edits or CSS driven by an external control, call `Orbit.refresh(container)` after your change; this synchronously refreshes that subtree.

These APIs describe the [current site build](/orbit-docs/introduction/installation/).


export const four = `
<div class="wrapper">
  <div class="bigbang">
    <div class="gravity-spot">
      <div class="orbit-4">
        <div class="satellite"></div>
      </div>
    </div>
  </div>
</div>
`;
export const fourCSS = `
.wrapper {
  border: 1px dotted var(--o-purple);
  height: 200px;
  width: 280px;
  margin: 5px;
  resize: both;
  overflow: auto;
}
.orbit-4{
  border-color: var(--o-cyan)
}
.satellite {
  border: none;
  background-color: var(--o-cyan)
}
`;
export const fourJS = `
const stopResizing = Orbit.resize('.wrapper')
// Call stopResizing() when this container unmounts.
`;

<ZumerPlay name={'four'}   htmlCode={four} cssCode={fourCSS} jsCode={fourJS}  theme={'theme-cyan'} height={'300px'} />



