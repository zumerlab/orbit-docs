---
title: Installation & quick start

---
import { PackageManagers } from 'starlight-package-managers'

import ZumerPlay from '/src/components/ZumerCode.astro';

Load **both the stylesheet and the JavaScript runtime**. CSS controls geometry and presentation; the runtime keeps ring indices, spacing and SVG paths in sync as elements and containers change.

## Use the same build as this site

The examples use the current Orbit source build. Download the matching files together:

- [orbit.min.css](/orbit-docs/orbit/orbit.min.css)
- [orbit.min.js](/orbit-docs/orbit/orbit.min.js)
- [orbit.mjs — ES module](/orbit-docs/orbit/orbit.mjs)
- [Build version, source commit and file checksums](/orbit-docs/orbit/manifest.json)

```html
<link rel="stylesheet" href="./orbit.min.css">
<script defer src="./orbit.min.js"></script>
```

The classic script registers `<o-arc>` and `<o-progress>` and exposes `window.Orbit`. With the ES module, load it from your browser entrypoint:

```js
import { Orbit } from './orbit.mjs';

// After the container mounts; call the returned function when it unmounts.
const stopResizing = Orbit.resize('#instrument-panel');
```

The current module is safe to import during server rendering. Its JavaScript must also run in the browser to activate the rendered markup.

## Install the published package

<PackageManagers pkg="@zumer/orbit" />

Load the stylesheet and import the package from your client entrypoint:

```js
import '@zumer/orbit/style';
import '@zumer/orbit';
```

:::note[Site build and npm release]
The source build used here includes the updated layout runtime, ES module exports and observer cleanup. The published npm release currently has the same `1.4.12` version number but predates those changes. Use the matching downloads above to reproduce this site's behavior; the manifest identifies the exact source commit. Publishing a new package release is a separate step.
:::

You can also load the published edition from a CDN. Pin the version so it changes only when you choose to update:

```html
<link rel="stylesheet" href="https://unpkg.com/@zumer/orbit@1.4.12/dist/orbit.min.css">
<script defer src="https://unpkg.com/@zumer/orbit@1.4.12/dist/orbit.min.js"></script>
```

## 🚀 Try Orbit now!

Just add `.bigbang` CSS class in a HTML element like `<div>`. Then use `.gravity-spot` class, and within it, add another element with `.orbit` class. Finally, inside `.orbit` element, add radial elements, such us: `.satellite`, `.vector`, `.side`, or `<o-arc>`, `<o-progress>` web components . 


### Playground starter example


export const bareboneHTML = 
`<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <link rel="stylesheet" href="https://zumerlab.github.io/orbit-docs/orbit/orbit.min.css" />
    <script crossorigin src="https://zumerlab.github.io/orbit-docs/orbit/orbit.min.js"></script>
    <title>Orbit quick start</title>
    <style>
    .satellite {
      background: var(--o-cyan-light);
      border: none;
    }
    </style>
  </head>
  <body>
    <div class="bigbang">
      <div class="gravity-spot">
        <div class="orbit">
          <div class="satellite grow-1x at-center"></div>
        </div>
        <div class="orbit-2 range-180 from-180">
          <o-arc shape="circle" class="shrink-50"></o-arc>
        </div>
        <div class="orbit-3 range-270 ">
          <o-arc shape="circle" class="shrink-50"></o-arc>
        </div>
      </div>
    </div>
  </body>
</html>`

<ZumerPlay name={'barebone'} htmlCode={bareboneHTML} height={'300px'} theme={'theme-cyan'} frameTemplate='barebone'> </ZumerPlay>

### Orbit and JS frameworks integrations

Orbit can be used in any framework that accepts web-components. Check our [specific section](/orbit-docs/integrations/react) that covers how to use **Orbit** with **Vue**, **React**, and **Svelte**. 

## Next steps

Congratulations! You have successfully integrated Orbit into your project. Now, you can explore more advanced features and customization options. Check out the full documentation for detailed guides and examples. 

Happy coding!

